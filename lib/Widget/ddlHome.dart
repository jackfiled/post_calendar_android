import 'package:flutter/material.dart';

class DDLHome extends StatelessWidget {
  const DDLHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("DDL"),
    );
  }
}
